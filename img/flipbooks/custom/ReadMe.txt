Drop your own flipbook sheets here as PNG files.
A sheet is one image holding every frame in a grid, left to right, top to bottom.
In the addon choose module type Flipbook, type the file name here, and set rows, columns, frames and fps.
